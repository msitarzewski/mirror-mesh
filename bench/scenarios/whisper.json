{
  "name": "whisper",
  "mode": "synthetic",
  "width": 640,
  "height": 360,
  "fps": 30,
  "frames": 120,
  "show_landmarks": true,
  "show_avatar_mask": true,
  "voice": true,
  "voice_duration_seconds": 4.0,
  "_comment": "M28 voice scenario. The bench CLI currently records frame events; voice path lives in the mirrormesh-listen binary and emits its own JSONL when wired. The `voice` flag is a forward-compat hint — see bench/scenarios/README.md and docs/voice-pipeline.md."
}
